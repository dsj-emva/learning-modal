"""EMVA lead scoring: formula (logistic regression), deal value, and evaluation.

Phase 0 split of ``baseline/emva_score.py`` into modules with no behaviour change.
Run ``python -m emva --data data/v1 --out DIR`` to reproduce the baseline.
"""
